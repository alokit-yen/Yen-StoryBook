import React from 'react';
import Spinner from './Spinner';

export default {
    title: 'Hiten/Spinner',
    component: Spinner,
    tags: ['autodocs'],
};

export const Default = () => <Spinner />;
